apiVersion: v1
kind: Service
metadata:
    name: commentmanagementservicewebapi
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: commentmanagementservicewebapi